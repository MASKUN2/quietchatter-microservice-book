apiVersion: apps/v1
kind: Deployment
metadata:
  name: book
  namespace: quietchatter
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  selector:
    matchLabels:
      app: book
  template:
    metadata:
      labels:
        app: book
    spec:
      nodeSelector:
        quietchatter.io/role: worker
      containers:
        - name: book
          image: maskun2/quietchatter-microservice-book:IMAGE_PLACEHOLDER
          ports:
            - containerPort: 8081
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: prod
            - name: SERVER_PORT
              value: "8081"
            - name: DB_URL
              value: jdbc:postgresql://quietchatter-db.cpq8skgs4o7i.ap-northeast-2.rds.amazonaws.com:5432/rdb-microservice-book
            - name: DB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: quietchatter-secrets
                  key: DB_USERNAME
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: quietchatter-secrets
                  key: DB_PASSWORD
            - name: NAVER_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: quietchatter-secrets
                  key: NAVER_CLIENT_ID
            - name: NAVER_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: quietchatter-secrets
                  key: NAVER_CLIENT_SECRET
            - name: INTERNAL_SECRET
              valueFrom:
                secretKeyRef:
                  name: quietchatter-secrets
                  key: INTERNAL_SECRET
            - name: KAFKA_BROKERS
              value: redpanda.quietchatter.svc.cluster.local:9092
            - name: SPRING_DATA_REDIS_HOST
              value: redis.quietchatter.svc.cluster.local
            - name: SPRING_DATA_REDIS_PORT
              value: "6379"
            - name: JAVA_OPTS
              value: "-Xms128m -Xmx256m -XX:MaxMetaspaceSize=128m -XX:+UseSerialGC -Xss512k"
          resources:
            limits:
              memory: 450Mi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8081
            initialDelaySeconds: 60
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: book
  namespace: quietchatter
spec:
  selector:
    app: book
  ports:
    - port: 8081
      targetPort: 8081
